<% if %i(edit new).include? @meal.ingredient_action %>
  <%= render "ingredients/#{@meal.ingredient_action}",
             meal: @meal,
             ingredient: @meal.ingredients[@meal.ingredient_index] || Ingredient.new %>
<% else %>
  <%= render 'form', meal: @meal %>
  <%= link_to 'Show', @meal %> |
  <%= link_to 'Back', meals_path %>
<% end %>
